<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.thm.draw4friends">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".Login.LoginActivity" android:label="@string/app_name" android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Home.HomeActivity" android:label="@string/app_name"/>

        <activity android:name=".Friendlist.FriendlistActivity" android:label="@string/friends"/>

        <activity android:name=".Paint.PaintCanvasActivity" android:label="@string/drawImage"/>

    </application>
</manifest>
